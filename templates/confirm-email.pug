doctype html
html
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Подтвердите Ваш Email
        style.
            body {
                margin: 0;
                padding: 0;
                background-color: #f5f5f5;
                font-family: Arial, sans-serif;
                font-size: 16px;
            }
            .wrapper {
                width: 100%;
                max-width: 600px;
                margin: 0 auto;
                background-color: #fff;
                border-radius: 5px;
                box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
            }
            .header {
                padding: 30px;
                text-align: center;
                background-color: #0077ff;
                color: #fff;
                border-radius: 5px 5px 0 0;
            }
            h1 {
                margin: 0;
                font-size: 28px;
                font-weight: bold;
            }
            .content {
                padding: 30px;
                text-align: center;
            }
            p {
                margin: 0;
                line-height: 1.5;
                color: #333;
            }
            a {
                display: inline-block;
                background-color: #0077ff;
                color: #fff;
                text-decoration: none;
                padding: 10px 20px;
                margin-top: 30px;
                border-radius: 5px;
                transition: background-color 0.3s ease;
            }
            a:hover {
                background-color: #0054b4;
            }
    body
        .wrapper
            .header
                h1 Подтвердите Ваш Email
            .content
                p Пожалуйста, подтвердите Ваш email адрес, нажав на кнопку ниже:
                a(href=`${confirmationUrl}`) Подтвердить Email
